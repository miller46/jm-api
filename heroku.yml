build:
  docker:
    web: Dockerfile
release:
  command:
    - sh
    - -c
    - migrate -path /migrations -database $DATABASE_URL up
  image: web
run:
  web:
    command:
      - api
    image: web
  worker:
    command:
      - worker
    image: web
