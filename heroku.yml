build:
  docker:
    web: Dockerfile
release:
  command:
    - migrate -path /migrations -database $DATABASE_URL up
  image: web
run:
  web:
    command:
      - api
    image: web
  worker:
    command:
      - worker
    image: web
